# Sync: Local Environment Variables
# Copy this file to .env and update values as needed
DOMAIN=localhost
STACK_NAME=sync-stack
ENVIRONMENT=local
PROJECT_NAME=Sync

# Security
SECRET_KEY=changethis
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis

# Database
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changethis

# Frontend
FRONTEND_HOST=http://localhost:5173
BACKEND_CORS_ORIGINS=["http://localhost:5173", "http://localhost:4173", "http://localhost"]

# Docker Images
DOCKER_IMAGE_BACKEND=sync-backend
DOCKER_IMAGE_FRONTEND=sync-frontend
TAG=latest

# SMTP (Mock)
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com

# Is file ka upyog local development environment setup karne ke liye kiya jata hai.
